name: fcp-fdm-event-publisher-stub-test

services:
  fcp-fdm-event-publisher-stub:
    build:
      target: development
    command: npm test
    volumes:
      - ./test:/home/node/test
      - ./coverage:/home/node/coverage
